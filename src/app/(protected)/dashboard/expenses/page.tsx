import { DashboardPlaceholderPage } from "@/components/ui/layout/dashboard";

export const metadata = {
  title: "Expenses — Fintrack",
};

export default function ExpensesPage() {
  return <DashboardPlaceholderPage title="Expenses" />;
}
