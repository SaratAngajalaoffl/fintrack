# Web documentation (Fintrack)

Docs here focus on the **Next.js** app: routing conventions, client data fetching, and UI-adjacent patterns.

| Document                                            | Description                                             |
| --------------------------------------------------- | ------------------------------------------------------- |
| [Frontend data fetching](frontend-data-fetching.md) | React Query setup and server-first fetching conventions |
| [Routes](routes.md)                                 | `getAppRoute` / `getApiRoute` registry approach         |

Source lives in **[fintrack-web](https://github.com/SaratAngajalaoffl/fintrack-web)** (paths in those files use `src/` at the repo root). When you work inside a **meta-repo** checkout, the same tree appears as **`web/`**.

Database and API field reference: [docs/api/data-model.md](../api/data-model.md).
